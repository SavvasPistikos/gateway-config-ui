import React from "react";

class SwaggerApi extends React.Component {
  render() {
    return (
      <div>
        <p>{props.api.info.title}</p>
      </div>
    );
  }
}

export default SwaggerApi;
